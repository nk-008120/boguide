# BioGuide — Content Creation Handoff (→ Molecular/Cell/Biochemistry Phase 2, or next subject)

**Written:** 2026-07-30. **Updated:** 2026-07-31 (after Molecular, Cell Biology & Biochemistry Phase 1 — content only, images/widgets still pending). **Purpose:** carry the established content pattern into a fresh conversation, so work can continue without re-deriving structure, depth calibration, or the Visual Reference format from scratch.

## Immediate next task

`content/resources/1-cell-molecular/` now has all 23 topic pages + landing page written (Phase 1 — text content only), replacing the old single tabbed `Biochemistry/_index.md` and two empty `Cell-Biology/`/`Molecular-Biology/` stubs entirely. **Phase 2 (static images) and Phase 3 (interactive widgets) have not started for this section.** This is the largest section on the site (23 pages, three tiers: Cell Biology, Molecular Biology, Biochemistry) and the natural next step in a fresh conversation, unless the user directs otherwise. See "Notable adaptations" below for specifics before starting Phase 2.

## Reference implementations

Eight sections are finished now (content-complete; image/widget completeness varies — see notes) — when in doubt about depth, structure, or tone, **read 1-2 pages directly** rather than relying on this summary:

- `content/resources/2-animal-anatomy/` (16 pages) — the original reference, fully complete (content + ~100 images + widgets). In particular:
  - `Human-Reproductive-System/_index.md` — the depth calibration sample the user approved (named cell types, staged/timed processes, specific mechanisms, not organ-name-level generality)
  - `Human-Circulatory-System/_index.md` — the Visual Reference format the user approved after two rounds of correction
- `content/resources/4-biosystematics/` (7 pages + landing) — second section, confirms the pattern generalizes beyond anatomy to a classification/analytical-method subject.
- `content/resources/3-animal-physiology/` (13 pages) — reference-quality, structurally consistent with the pattern.
- `content/resources/6-plant-anatomy/` (10 pages + landing, weight 6) — confirms the pattern generalizes to a subject with real internal comparative complexity (monocot vs. dicot).
- `content/resources/9-ethology/` (9 pages + landing, weight 9) — confirms the pattern generalizes to an example/case-study-driven subject; introduced the `<!-- VIDEO: ... -->` marker convention.
- `content/resources/15-evolution/` (11 pages + landing, weight 15) — confirms the pattern as a synthesis/deferral layer over two prior sections (Biosystematics, Ethology) at once.
- `content/resources/5-plant-physiology/` — complete, referenced repeatedly by later sections (e.g. Calvin cycle page cross-linked from the new Bioenergetics page below).
- **`content/resources/1-cell-molecular/` (23 pages + landing, weight 1) — newest, written 2026-07-31, Phase 1 (content) only.** By far the largest section (previous max was 16 pages), split into three tiers matching the three pre-existing-but-empty subdirectory names (Cell Biology, Molecular Biology, Biochemistry), now flattened into 23 sibling topic directories directly under the section root (tiers are landing-page card groupings only, per the established no-folder-nesting convention). Notable adaptations, worth reusing:
    - **Content reuse from a pre-existing monolithic tabbed page**: the old `Biochemistry/_index.md` (1,146 lines, `{{< tabs >}}`-based) had real IBO-depth content in 3 of its 5 tabs (Amino Acids, Proteins, Enzyme Kinetics) — these were split one-to-one into `Amino-Acids-Protein-Chemistry/`, `Protein-Structure-Folding-Function/`, and `Enzyme-Kinetics-Regulation/`, restructured into the standard template, with the Appendix tab's derivations folded into the relevant page's Key Concepts and the Questions tab's 5 problems redistributed into the matching new page's Practice Problems by topic (not kept as a standalone page). Worth checking any future subject for a similar old-format page to mine before writing from scratch.
    - **Pre-existing unlogged images**: `static/MCBBPICS/` (20 files) already existed and was already referenced inline in the old Biochemistry tabs, but predates the current sourcing/logging workflow entirely — **none of the 20 are in `image-sources.md`**. Phase 2 for this section needs a backfill-logging pass for these specifically (view each, confirm/correct the candidate placements already noted in each new page's Visual Reference **Static** list, log properly), in addition to sourcing the ~15+ genuinely new images the other 20 pages need.
    - **Cross-section scope boundary, explicitly negotiated with the user via AskUserQuestion before writing**: this section's Molecular Biology tier (`DNA-Structure-Replication`, `DNA-Repair-Recombination`, `Transcription-RNA-Processing`, `Translation-Genetic-Code`, both `Gene-Regulation-*` pages) now fully owns DNA replication/repair, transcription, translation, and gene regulation mechanisms. `content/resources/7-genetics/_index.md` (not yet rewritten to the current pattern — still an old-style TOC page with `(#)` placeholder links) independently lists these same topics as its own future Units III-IV, including two placeholder widgets ("DNA Replication Animation," "Transcription & Translation Animation") that duplicate widgets now planned for this section instead. **When Genetics is eventually rewritten, trim its Units III-IV to cross-link into `1-cell-molecular/` rather than re-deriving this content** — same deferral pattern already used between Evolution and Biosystematics/Ethology. Also: `Cell-Cycle-Mitosis-Meiosis/_index.md` here covers mitosis/meiosis *chromosome mechanics* only; Genetics should own the *genetic consequences* (independent assortment, recombination frequency, linkage mapping) when it's rewritten — don't duplicate either direction.
    - **Metabolism scope, explicitly confirmed with the user rather than assumed**: `Bioenergetics-Central-Metabolism/_index.md` is deliberately an IBO-scoped overview (glycolysis/TCA/oxidative phosphorylation at tested depth), not full Lehninger chapter-by-chapter metabolism (which would have added 5-6 more pages) — this was a direct response to a survey complaint ("depth is unknown, people just keep reading hundreds of pages"). If a future session is tempted to expand this page into full metabolic-pathway depth, check with the user first rather than assuming more depth is automatically better.
    - **Survey-driven additions**: `Recombinant-DNA-Biotechnology-Techniques/_index.md` (cloning, PCR, blotting, sequencing, CRISPR) and `Biomolecular-Analytical-Techniques/_index.md` (SDS-PAGE, chromatography, spectrophotometry) exist specifically because BioGuide's own 12-response student survey named these techniques directly. Worth checking survey results again before finalizing any future section's scope.
    - Followed the plan-then-write flow cleanly end to end, with two AskUserQuestion clarifications before writing (metabolism depth, Genetics ownership) rather than one — worth defaulting to more clarifying questions, not fewer, when a section this size touches multiple other sections' territory.

## Phase 1: Content (plan first, then write)

Do **not** write Markdown until the user approves a subtopic breakdown — a wrong first guess at structure has been corrected multiple times across sections (depth, tier ratio, and cross-section ownership boundaries have all needed a round of correction at some point). Propose a subtopic list, ask what natural tiers/ratio the subject has, and — as of the Molecular/Cell/Biochemistry pass — also explicitly ask about any scope overlap with sections not yet rewritten (check `content/resources/*/​_index.md` for old-style TOC pages that list topics your new section might also cover) and about depth/breadth trade-offs if the subject is large enough that "textbook-complete" and "exam-scoped" would produce meaningfully different page counts.

**Depth bar**: same throughout — roughly first-year-university depth, named cell types/molecules and their specific roles, staged/timed processes, specific structural/chemical mechanisms, not name-level generality. Read `content/resources/2-animal-anatomy/Human-Reproductive-System/_index.md` as the calibration sample if unsure.

**Front matter / page template**: reuse `layouts/shortcodes/topic-meta.html` as-is (no changes needed), same `difficulty`/`prerequisites`/`syllabus_tags` fields, same section order (Overview → Key Concepts → Comparative Structures → Common Exam Questions → Visual Reference → Practice Problems), one flat subdirectory + `_index.md` per topic directly under the section root (never nested by tier, never a monolithic tabbed page), split further whenever a topic proves too dense to share a page.

## Phase 2: Static images

Read `C:\NK\boguide\visual-reference-workflow.md` if it still exists (confirm it's still there and current before relying on it). Key points that have held true in practice across every section so far:

- Propose a batch of Static items per page (filename + description each) for the user to source.
- **Two sourcing variants happen in practice**: (a) the user pastes source *pages* (not files) and expects you to `WebFetch` them to locate/verify the actual image + license before asking permission to download; (b) the user just saves files directly into `static/<SECTION>PICS/` themselves and says "saved" — check the directory before doing anything else, since this became the dominant pattern in later sections.
- **Always view every image with `Read` before captioning** — never caption from a filename or the user's one-line source description alone. Several "obvious" matches turned out to be mismatches (wrong topic entirely) or had visible copyright notices baked into the image that needed flagging. This applies doubly to the pre-existing, never-verified `MCBBPICS` images for the Molecular/Cell/Biochemistry section (see above).
- Asset folder convention: `static/<SECTION>PICS/` — `ECOLOGYPICS`, `MCBBPICS` (already exists, 20 unlogged files), `BIOINFOPICS`, `ANATOMYPICS`, `ANIMALPHYSIOPICS`, `PLANTPHYSIOPICS`, `PLANTANATPICS`, `EVOLUTIONPICS`. No new folder needed for Molecular/Cell/Biochemistry — `MCBBPICS` already exists.
- **Gotchas hit repeatedly, budget for them**: SVG and AVIF files don't render via the `Read` tool — rasterize to PNG first (Python/Pillow works directly for AVIF; for SVG, use the Browser tool to fetch+draw the SVG onto an `OffscreenCanvas` and export as a PNG data URL). Filenames with spaces/parentheses need renaming to something URL-safe before referencing them in Markdown. The user sometimes saves files into the *wrong* section's picture folder — check there if a file isn't where expected.
- Log every image in `C:\NK\boguide\image-sources.md` (append rows, don't rewrite the file) with a status: `OK`/`OK (likely)` for confirmed-open licenses, `CHECK` for unconfirmed, `CONFIRMED COPYRIGHT` for a visible copyright/signature baked into the image itself (flag clearly, insert anyway per the user's standing preference, but mark it must not go to production unlicensed). If an image doesn't actually match its spec, insert it anyway with an honest caption explaining the mismatch.
- After inserting, remove the placed items from the page's `**Static**` Visual Reference list (leave a one-line "(placed inline above)" note) and verify live: `curl` every image URL for 200, and grep the rendered page HTML to confirm each image lands under the correct heading.
- The dev server (`hugo server --port 1313`) can silently die mid-session — if a `curl` to `127.0.0.1:1313` returns `000`, just restart it (`nohup hugo server --port 1313 > /tmp/hugo-server.log 2>&1 &`, wait ~25s for the initial build).

## Phase 3: Interactive widgets

No build step, no npm, everything is raw HTML/CSS/JS/Plotly embedded directly in the Markdown content file (or, per `visual-reference-workflow.md`'s stated preference for standalone widgets, a `.html` file under `static/` wired in via `{{< iframe >}}` — check which convention the file currently documents before starting, since both patterns exist on the live site). `hugo.toml` already has `unsafe = true` in the goldmark renderer config, so raw HTML passes through with no config changes needed. Use Plotly for anything quantitative, vanilla JS manipulating an inline SVG for click-through/hover/slider/toggle interactions. Verify every widget by actually clicking/dragging it in the Browser tool and confirming the DOM/visual state changes — not just confirming the page loads. Move each built widget's description out of the Visual Reference `**Interactive**` list into the body text near the concept it illustrates.

**For Molecular/Cell/Biochemistry specifically**, every topic page's `## Visual Reference` → `**Interactive**` section already has a concrete widget proposal tagged `(built later — see project workflow)` — read each page rather than re-deriving ideas. Two are flagged higher priority than the rest: the DNA replication fork animation (`DNA-Structure-Replication`) and the coupled transcription/translation animation (`Transcription-RNA-Processing` + `Translation-Genetic-Code`, meant to be built as one connected pair), because building them here directly resolves the widget duplication risk with Genetics' own placeholder units noted above.

## Suggested pacing

Either do Phase 1 (full content, all pages) first and get it approved, then work through Phases 2-3 page by page — or do all three phases for one page at a time before moving to the next page. Either way: **don't skip the subtopic-breakdown approval step at the very start**, and for any section large enough to plausibly overlap another not-yet-rewritten section, don't skip asking about the ownership boundary either.
