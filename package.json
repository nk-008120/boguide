{
  "name": "biounified",
  "private": true,
  "scripts": {
    "dev:hugo": "cd apps/boguide && hugo server -D",
    "dev:react": "cd apps/bionuggets && pnpm dev",
    "build:hugo": "cd apps/boguide && hugo --minify",
    "build:react": "cd apps/bionuggets && pnpm build",
    "build": "pnpm build:hugo && pnpm build:react"
  }
}