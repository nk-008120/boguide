# CLAUDE.md

This repository has a binding AI-use policy that applies to every Claude
session working here. Read `AGENTS.md` in full before making any change —
it covers restricted content zones (BiOClash), mandatory logging to
`AI_INVOLVEMENT_LOG.csv`, and the disclosure-not-concealment policy that
took effect 2026-08-2. `AI_ATTESTATION.txt` is the underlying public
attestation both files serve.
